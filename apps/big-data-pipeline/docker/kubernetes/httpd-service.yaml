apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.prod.yaml -o docker/kubernetes
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: httpd
  name: httpd
spec:
  ports:
    - name: "8008"
      port: 8008
      targetPort: 80
  selector:
    io.kompose.service: httpd
